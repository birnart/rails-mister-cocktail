<h1>All Cocktails</h1>
<ul>
  <% @cocktails.each do |cocktail| %>
    <li>
      <%= link_to "#{cocktail.name}", "cocktails/#{cocktail.id}" %>
    </li>
  <% end %>
</ul>
<hr>
<%= link_to "+Add a new Cocktail", "cocktails/new" %>
